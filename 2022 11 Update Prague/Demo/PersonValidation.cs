namespace Demo;

public static class PersonValidation
{
    //public static Person Complete(this Person? p) =>
    //    p is null ? throw new ArgumentNullException(nameof(p))
    //    : p.FirstName is null ? throw new ArgumentException(nameof(p.FirstName))
    //    : p.LastName is null ? throw new ArgumentException(nameof(p.LastName))
    //    : p;

    //public static IEnumerable<Person> Complete(
    //    this IEnumerable<Person?>? persons) =>
    //    persons is null ? throw new ArgumentNullException(nameof(persons))
    //    : persons.Select(Complete);
}
